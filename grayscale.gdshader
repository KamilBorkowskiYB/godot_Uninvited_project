shader_type canvas_item;
render_mode unshaded;

uniform sampler2D fadeOutView;

void fragment() {
	//COLOR = texture(fadeOutView, UV);
	//float lumi = (COLOR.r + COLOR.g + COLOR.b) / 3.0;
	//COLOR.rgb = vec3(lumi);
}

